{
  "private": true,
  "scripts": {
    "build": "chmod +x ./vercel-build.sh && ./vercel-build.sh"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "vercel-php": "^0.6.0"
  }
} 